# --- web server ---
fastapi==0.141.1
uvicorn==0.52.1
pydantic==2.13.5
python-dotenv==1.2.3

# --- LangChain / LangGraph core ---
langchain_core==1.6.1
langchain_text_splitters==1.1.2
langchain_community==0.4.2
langgraph==1.2.11

# --- chat persistence (same-server PostgreSQL) ---
# AsyncPostgresSaver uses psycopg 3 (async), NOT psycopg2.
langgraph-checkpoint-postgres==3.1.2
psycopg[binary]==3.3.4
psycopg-pool==3.3.1

# --- model providers ---
langchain_google_genai==4.4.0
langchain_deepseek==1.1.0

# --- vector store / embeddings (policy + FAQ retrieval) ---
langchain_chroma==1.1.0
chromadb==1.5.9
langchain_huggingface==1.2.2
sentence-transformers==5.7.0

# --- store business data (backend SQL Server) ---
# NOTE: pyodbc also needs the system-level "ODBC Driver 18 for SQL Server"
# installed on the host - pip alone is not enough. See DEPLOYMENT.md.
pyodbc==5.3.0
